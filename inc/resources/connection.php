<?php 
$dbhost = "localhost";
$dbuser = "root";
$dbpass = "";
$dbname = "portfolio";

$conn =  mysqli_connect($dbhost, $dbuser, $dbpass, $dbname,);

/* if ($conn) {
    echo "Connected to database";
} else {
    echo "Connection failed";
} */